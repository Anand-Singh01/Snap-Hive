import React from "react";

interface postData {
  postId: string;
}
const EditPost: React.FC<postData> = ({ postId }) => {
  return (
    <>
      {/* <PostForm /> */}
    </>
  );
};

export default EditPost;
